import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#c200c2ff',
            light: '#fff'
        },
        secondary: { main: '#11112fff' },
    },
    typography: {
        fontFamily: 'Roboto'
    }
});