import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#E000DF',
            light: '#fff'
        },
        secondary: { main: '#11112fff' },
    },
    typography: {
        fontFamily: 'Roboto'
    }
});